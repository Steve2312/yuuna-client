type SongMetadata = {
    audio: string,
    artist: string,
    title: string,
    version: string,
    source: string,
    duration: number,
    creator: string,
    bpm: number,
    beatmapset_id: number,
    id: string
    date_added: number,
}

export default SongMetadata